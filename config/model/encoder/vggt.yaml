name: vggt

img_size: 256
patch_size: 16
embed_dim: 1024
intrinsics_embed_type: 'token'

opacity_mapping:
  initial: 0.0
  final: 0.0
  warm_up: 1

num_surfaces: 1

gaussian_adapter:
  gaussian_scale_min: 0.5
  gaussian_scale_max: 15.0
  sh_degree: 4

downscale_factor: 4
pose_free: True